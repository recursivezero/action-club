name: Bump Version
run-name: bump version and update changelog

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - name: Run bump-version action
        uses: recursivezero/action-club/.github/actions/bump-version
        with:
          node-version: "18"
          enable-bump: "true"
          enable-changelog: "true" # toggle changelog update
